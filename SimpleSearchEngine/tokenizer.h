#ifndef TOKENIZER_H
#define TOKENIZER_H

#include <string>
#include <vector>

std::string toLower(const std::string& str);
std::vector<std::string> tokenize(const std::string& text);

#endif // TOKENIZER_H
